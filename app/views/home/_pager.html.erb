<div class="row">
  <div class="col-sm-6">
    <%#= paginate items, :remote => true #%>
    <%#= link_to_next_page items, t("story.more"), remote: true, class: "btn btn-default btn-more" #%>
    <%= link_to_previous_page items, t("story.prev"), class: "btn btn-primary #{class_name}" %>
  </div>

  <div class="col-sm-6">
    <%#= paginate items, :remote => true #%>
    <%#= link_to_next_page items, t("story.more"), remote: true, class: "btn btn-default btn-more" #%>
    <%= link_to_next_page items, t("story.next"), class: "btn btn-primary #{class_name}" %>
  </div>
</div>

<br/>
<br/>